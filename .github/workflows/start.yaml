name: Linting
on: workflow_dispatch
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: setup
        uses: actions/checkout@v3
      - name: install dep
        run: npm i
      - name: greeting
        run: echo hellow
